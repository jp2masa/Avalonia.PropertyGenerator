<Project>

    <PropertyGroup>
        <PackageVersion Condition="'$(PackageVersionSuffix)' != ''">$(PackageVersion)$(PackageVersionSuffix)</PackageVersion>
    </PropertyGroup>

    <PropertyGroup>
        <BaseDescription>Avalonia.PropertyGenerator generates the appropriate CLR members for Avalonia property definitions.</BaseDescription>
    </PropertyGroup>

    <PropertyGroup>
        <Authors>jp2masa</Authors>
        <Copyright>Copyright © $([System.DateTime]::Now.Year) jp2masa</Copyright>
        <!--<PackageIconUrl>https://raw.githubusercontent.com/jp2masa/Avalonia.PropertyGenerator/master/Icon.png</PackageIconUrl>-->
        <PackageProjectUrl>http://github.com/jp2masa/Avalonia.PropertyGenerator</PackageProjectUrl>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <PackageRequireLicenseAcceptance>True</PackageRequireLicenseAcceptance>
        <PackageTags>avalonia property styled direct attached source generator dotnet core csharp</PackageTags>
        <RepositoryType>git</RepositoryType>
        <RepositoryUrl>https://github.com/jp2masa/Avalonia.PropertyGenerator.git</RepositoryUrl>
    </PropertyGroup>

</Project>
